export enum AgentStatsTypes {
  ALL = 1,
  CPU = 2,
  DISK = 3,
  MEMORY = 4,
  NET = 5,
}

export enum AgentStatus {
  AGENT_STATUS_UNSPECIFIED = 0,
  REGISTRED = 1,
  RUNNED = 2,
  DISCONNECTED = 3,
  UNREGISTRED = 4,
}
